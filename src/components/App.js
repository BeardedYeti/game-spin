import React from 'react';
import { connect } from 'react-redux';
import style from '../stylesheets/style.scss';

// @connect(
// 	state => ({
// 		movies: state.movies,
// 		loading: state.loading,
// 	}),
// 	dispatch => ({
// 		refresh: () => dispatch({type: 'GET_MOVIE_DATA'}),
// 	}),
// )
// Need to render props

const App = () => (
	<div>
    	<h2>React Test test</h2>
 	</div>
);

export default App;
